# AGENTS.md

## 项目协作规则

1. 每次开始项目前，先检查当前目录、开发工具、开发环境、关键配置和版本，再制定计划。
2. 每次只做一个小任务，不一次性生成完整项目。
3. 每次开始前说明任务目标、技术点、涉及文件和验证方式。
4. 修改代码前先检查项目结构，不随意推翻已有代码。
5. 数据库密码、API Key、云服务器密码等敏感信息必须使用环境变量或示例配置，不能写死到仓库。
6. 每完成一个功能后，指导运行、测试、查看 `git status`、提交和推送。
7. 每次给出规范 commit message。
8. 初始化或维护 Git 仓库时，将构建产物、依赖目录、本地配置、日志、密钥和临时文件排除。

## 当前项目目标

AgriMind 智慧农业土壤检测与 AI 决策平台。

技术路线：

- Java 17 + Spring Boot 3 + MyBatis-Plus + MySQL + Redis
- Vue 3 + Vite + Element Plus + ECharts
- 大模型 API 土壤分析报告
- RAG 农业知识库问答
- Agent 工具调用
- GitHub、Docker、Nginx、云服务器和云数据库部署
